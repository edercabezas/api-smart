{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "migrate:refresh": "php artisan migrate:refresh --seed"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
